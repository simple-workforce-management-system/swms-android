<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="net.roughdesign.swms.swmsandroid">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".DashboardActivity"
                android:label="@string/title_activity_dashboard"
                android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
                android:name=".clients.ClientDetailActivity"
                android:label="@string/title_activity_client_detail"
                android:parentActivityName=".clients.ClientListActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="net.roughdesign.swms.swmsandroid.clients.ClientListActivity" />
        </activity>
        <activity
                android:name=".clients.ClientListActivity"
                android:label="@string/title_activity_client_list"
                android:parentActivityName=".StartupActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value="net.roughdesign.swms.swmsandroid.StartupActivity" />
        </activity>
        <activity android:name=".StartupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>